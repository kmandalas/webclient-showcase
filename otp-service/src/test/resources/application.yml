spring:
  flyway:
    url: ${DB_URL}
    password: ${DB_PASSWORD}
    baseline-on-migrate: true
    user: ${DB_USERNAME}
  r2dbc:
    url: ${DB_URL_R2DBC}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

external:
  services:
    notifications: http://localhost:8005/notifications
    number-information: http://localhost:8006/number-information